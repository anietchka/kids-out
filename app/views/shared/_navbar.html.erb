<div id="content-navbar" class="navbar fixed-bottom">
  <% if user_signed_in? %>
    <div class="nav-item d-flex flex-column align-items-center ">
      <%= link_to root_path, class: "nav-link", data: {turbolinks: false} do%>
        <div class="navbar-icon d-flex flex-column align-items-center <%= 'active' if current_page?(root_path) ||  current_page?(offers_path)%>">
          <i class="fas fa-search pt-2"></i>
          <p class="pt-2">Explorer</p>
        </div>
      <% end %>
    </div>
    <div class="nav-item d-flex flex-column align-items-center">
      <%= link_to user_meetups_meetups_path, class: "nav-link" do%>
          <div class=" navbar-icon d-flex flex-column align-items-center  <%= 'active' if current_page?(user_meetups_meetups_path) %>">
            <i class="far fa-calendar-alt pt-2"></i>
            <p class="pt-2">Meetups</p>
          </div>
      <% end %>
    </div>
    <div class="nav-item d-flex flex-column align-items-center ">
      <%= link_to bookmarks_path, class: "nav-link" do%>
          <div class="navbar-icon d-flex flex-column align-items-center <%= 'active' if current_page?(bookmarks_path) %>">
            <i class="far fa-heart pt-2"></i>
            <p class="pt-2">Favoris</p>
          </div>
      <% end %>
    </div>
    <div class="nav-item d-flex flex-column align-items-center">
      <%= link_to profile_path, class: "nav-link" do%>
        <div class="navbar-icon d-flex flex-column align-items-center <%= 'active' if current_page?(profile_path) %>">
          <i class="far fa-user-circle pt-2"></i>
          <p class="pt-2">Profil</p>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="nav-item d-flex flex-column align-items-center ">
      <%= link_to root_path, class: "nav-link", data: {turbolinks: false} do%>
        <div class="navbar-icon d-flex flex-column align-items-center <%= 'active' if current_page?(root_path) ||  current_page?(offers_path)%>">
          <i class="fas fa-search pt-2"></i>
          <p class="pt-2">Explorer</p>
        </div>
      <% end %>
    </div>
    <div class="nav-item d-flex flex-column align-items-center ">
      <%= link_to new_user_registration_path, class: "nav-link" do%>
          <div class="navbar-icon d-flex flex-column align-items-center <%= 'active' if current_page?(new_user_registration_path) %>">
            <i class="fas fa-user-plus pt-2"></i>
            <p class="pt-2">Inscription</p>
          </div>
      <% end %>
    </div>
    <div class="nav-item d-flex flex-column align-items-center ">
      <%= link_to new_user_session_path, class: "nav-link" do%>
          <div class="navbar-icon d-flex flex-column align-items-center <%= 'active' if current_page?(new_user_session_path) %>">
            <i class="fas fa-sign-in-alt pt-2"></i>
            <p class="pt-2">Connection</p>
          </div>
      <% end %>
    </div>
  <% end %>
</div>
